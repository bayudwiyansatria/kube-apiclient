{{- range .Values.envFrom }}
{{- if .secretRef }}
apiVersion: v1
kind: Secret
metadata:
  name: {{ .secretRef.name }}
  labels:
    {{- include "kube-apiclient.labels" $ | nindent 4 }}
type: Opaque
stringData:
  {{- with .data }}
  {{- toYaml . | nindent 2 }}
  {{- end }}
---
{{- end }}
{{- end }}