apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "bayudwiyansatria-kubernetes-service.fullname" . }}-test-connection"
  labels:
    {{- include "bayudwiyansatria-kubernetes-service.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: busybox
      image: bayudwiyansatria/busybox:latest
      command: ['wget']
      args: ['{{ include "bayudwiyansatria-kubernetes-service.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
