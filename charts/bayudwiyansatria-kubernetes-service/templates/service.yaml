apiVersion: v1
kind: Service
metadata:
  name: {{ include "bayudwiyansatria-kubernetes-service.fullname" . }}
  labels:
    {{- include "bayudwiyansatria-kubernetes-service.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "bayudwiyansatria-kubernetes-service.selectorLabels" . | nindent 4 }}
