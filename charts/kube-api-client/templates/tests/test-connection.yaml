apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "kube-apiclient.fullname" . }}-test-connection"
  labels:
    {{- include "kube-apiclient.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: busybox
      image: bayudwiyansatria/busybox:latest
      command: ['wget']
      args: ['{{ include "kube-apiclient.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
