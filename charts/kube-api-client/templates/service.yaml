apiVersion: v1
kind: Service
metadata:
  name: {{ include "kube-apiclient.fullname" . }}
  labels:
    {{- include "kube-apiclient.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "kube-apiclient.selectorLabels" . | nindent 4 }}
